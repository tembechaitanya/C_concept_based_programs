#include<stdio.h>

int main()
{
    int iNo1 = 10;
    int iNo2 = 11;
    int main = 0;

    main = iNo1 + iNo2;

    printf("Addition is %d \n", main);
    // printf("size of iAns is %d bytes \n", sizeof(iAns));

    // printf("size of main function is %d bytes \n", sizeof(main));
    // printf("size of printf function is %d bytes \n", sizeof(printf));
    // printf("size of printf function is %d bytes \n", sizeof(scanf));

    return 0;
}